"use client";
import Link from "next/link";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Analysis, coverage, gaps, loadAnalyses } from "@/lib/analysis-store";
export default function GapPage(){const [analysis]=useState<Analysis | undefined>(() => loadAnalyses()[0]);if(!analysis)return <main className="min-h-screen bg-background px-6 py-10"><div className="mx-auto max-w-3xl"><h1 className="text-3xl font-semibold">Täckning & Gap</h1><div className="mt-8 rounded-xl border border-dashed border-border p-10 text-center"><p className="font-medium">Ingen analys att visa</p><p className="mt-2 text-sm text-muted-foreground">Skapa en analys och börja svara på frågorna.</p><Button asChild className="mt-5"><Link href="/analysis/new">Skapa analys</Link></Button></div></div></main>;const items=gaps(analysis);return <main className="min-h-screen bg-background px-6 py-10"><div className="mx-auto max-w-4xl"><div className="flex items-center justify-between"><div><p className="font-mono text-xs uppercase tracking-[0.18em] text-muted-foreground">Analysöversikt</p><h1 className="mt-2 text-3xl font-semibold">Täckning & Gap</h1></div><Button asChild variant="outline"><Link href={`/analysis/${analysis.id}/1`}>Fortsätt svara</Link></Button></div><div className="mt-8 rounded-2xl border border-border bg-card p-6"><div className="flex items-end justify-between"><div><p className="text-sm text-muted-foreground">Täckning</p><p className="mt-2 text-5xl font-semibold">{coverage(analysis)}%</p></div><p className="text-sm text-muted-foreground">Besvarade synliga frågor</p></div><div className="mt-5 h-3 overflow-hidden rounded-full bg-muted"><div className="h-full rounded-full bg-primary transition-all" style={{width:`${coverage(analysis)}%`}} /></div></div><section className="mt-8"><h2 className="text-xl font-semibold">Gap ({items.length})</h2><p className="mt-1 text-sm text-muted-foreground">Frågor besvarade med Nej eller Delvis.</p>{items.length?<div className="mt-4 space-y-3">{items.map((item)=><div key={item.id} className="rounded-xl border border-border bg-card p-5"><p className="font-medium">{item.label}</p><p className="mt-2 text-sm text-muted-foreground">{analysis.answers[item.id] === "no" ? "Nej" : "Delvis"}{analysis.notes[item.id] ? ` · ${analysis.notes[item.id]}` : ""}</p></div>)}</div>:<div className="mt-4 rounded-xl border border-dashed border-border p-8 text-center text-sm text-muted-foreground">Inga Gap ännu.</div>}</section></div></main>}
