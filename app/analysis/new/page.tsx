"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { Button } from "@/components/ui/button";
import { newAnalysis, saveAnalysis, SystemScope } from "@/lib/analysis-store";
export default function NewAnalysisPage(){const router=useRouter();const [company,setCompany]=useState("");const [systems,setSystems]=useState<SystemScope[]>(["K","M"]);function toggle(system:SystemScope){setSystems((current)=>current.includes(system)?current.filter((item)=>item!==system):[...current,system])}function start(){const analysis=newAnalysis(company);analysis.systems=systems;saveAnalysis(analysis);router.push(`/analysis/${analysis.id}/1`)}return <main className="min-h-screen bg-background px-6 py-10"><div className="mx-auto max-w-2xl"><p className="font-mono text-xs uppercase tracking-[0.2em] text-muted-foreground">Ny analys</p><h1 className="mt-3 text-4xl font-semibold tracking-tight">Börja med företagets omfattning</h1><p className="mt-4 leading-7 text-muted-foreground">Välj vilka system som ska ingå. Kvalitet + miljö visas som KM-frågor när minst ett system är valt.</p><div className="mt-8 space-y-6 rounded-2xl border border-border bg-card p-6"><label className="grid gap-2 text-sm font-medium">Företagets namn<input value={company} onChange={(e)=>setCompany(e.target.value)} className="h-11 rounded-lg border border-input bg-background px-3 font-normal outline-none focus:ring-2 focus:ring-ring" placeholder="Skriv företagsnamn" /></label><fieldset><legend className="text-sm font-medium">System som ska ingå</legend><div className="mt-3 flex gap-3">{(["K","M"] as SystemScope[]).map((system)=><button type="button" key={system} onClick={()=>toggle(system)} className={`rounded-lg border px-5 py-3 text-sm font-medium ${systems.includes(system)?"border-primary bg-primary text-primary-foreground":"border-border"}`}>{system === "K" ? "Kvalitet (K)" : "Miljö (M)"}</button>)}</div></fieldset><div className="flex justify-end gap-3"><Button variant="outline" onClick={()=>router.push("/")}>Avbryt</Button><Button disabled={!systems.length} onClick={start}>Skapa analys</Button></div></div></div></main>}
